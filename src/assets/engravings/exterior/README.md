Exterior engraving assets for Ringconf 2.7.5.

The card SVGs are local UI previews only. The masks are deterministic sample motifs for the
3D preview. They are not customer uploads and do not contain production artwork.

No recording, camera, file upload, blob, base64 customer file, or remote asset is used here.
